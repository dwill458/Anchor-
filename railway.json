{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install --legacy-peer-deps && npm run build --prefix backend"
  },
  "deploy": {
    "startCommand": "cd backend && npm run start"
  }
}
