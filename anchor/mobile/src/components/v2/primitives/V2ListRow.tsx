import React from 'react';
import { Pressable, StyleSheet, Text, View, type StyleProp, type ViewStyle } from 'react-native';
import { ChevronRight } from 'lucide-react-native';
import { colors, radii, spacing, typography } from '@/theme/v2';
type Props = { icon?: React.ReactNode; title: string; subtitle?: string; trailing?: React.ReactNode; onPress?: () => void; accent?: string; disclosure?: boolean; variant?: 'canvas' | 'grouped'; accessibilityLabel?: string; style?: StyleProp<ViewStyle>; testID?: string };
export function V2ListRow({ icon, title, subtitle, trailing, onPress, accent, disclosure = false, variant = 'canvas', accessibilityLabel, style, testID }: Props) { const content = <><View style={[styles.icon, accent && { backgroundColor: `${accent}18` }]}>{icon}</View><View style={styles.copy}><Text style={styles.title}>{title}</Text>{subtitle ? <Text style={styles.subtitle}>{subtitle}</Text> : null}</View><View style={styles.trailing}>{trailing}{disclosure ? <ChevronRight size={18} color={colors.text.secondary} /> : null}</View></>; return <Pressable testID={testID} onPress={onPress} disabled={!onPress} accessibilityRole={onPress ? 'button' : undefined} accessibilityLabel={accessibilityLabel ?? title} style={({ pressed }) => [styles.base, variant === 'grouped' && styles.grouped, pressed && onPress && styles.pressed, style]}>{content}</Pressable>; }
const styles = StyleSheet.create({ base: { minHeight: 56, flexDirection: 'row', alignItems: 'center', gap: spacing[3], paddingVertical: spacing[2] }, grouped: { paddingHorizontal: spacing[3], borderRadius: radii.md, backgroundColor: colors.grouped }, pressed: { opacity: 0.72 }, icon: { width: 36, height: 36, borderRadius: radii.md, alignItems: 'center', justifyContent: 'center' }, copy: { flex: 1, gap: 2 }, title: { ...typography.labelLG, color: colors.text.primary }, subtitle: { ...typography.bodySM, color: colors.text.secondary }, trailing: { flexDirection: 'row', alignItems: 'center', gap: spacing[2] } });
