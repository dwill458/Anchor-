import React from 'react';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { colors, radii, spacing, typography } from '@/theme/v2';
type Option = { value: string; label: string; accessibilityLabel?: string };
export function V2SegmentedControl({ options, value, onChange, accessibilityLabel }: { options: Option[]; value: string; onChange: (value: string) => void; accessibilityLabel: string }) { return <View accessibilityRole="radiogroup" accessibilityLabel={accessibilityLabel} style={styles.control}>{options.map((option) => { const selected = option.value === value; return <Pressable key={option.value} onPress={() => onChange(option.value)} accessibilityRole="radio" accessibilityLabel={option.accessibilityLabel ?? option.label} accessibilityState={{ selected }} style={[styles.option, selected && styles.selected]}><Text style={[styles.label, selected && styles.selectedLabel]}>{option.label}</Text></Pressable>; })}</View>; }
const styles = StyleSheet.create({ control: { minHeight: 44, flexDirection: 'row', padding: 3, borderRadius: radii.md, backgroundColor: colors.grouped }, option: { minHeight: 44, flex: 1, alignItems: 'center', justifyContent: 'center', borderRadius: radii.sm, paddingHorizontal: spacing[2] }, selected: { backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border.subtle }, label: { ...typography.labelMD, color: colors.text.secondary, textAlign: 'center' }, selectedLabel: { color: colors.text.primary } });
