# Anchor 1.5 HTML references

`package/` is the supplied 24-screen HTML reference pack plus its `index.html`
preview. These files are design references only and are intentionally outside
`anchor/mobile`, so Expo does not package them with the native application.

The source of truth for shipped behavior is the native implementation under
`anchor/mobile/src`.
